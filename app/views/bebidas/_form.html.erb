<%= form_with(model: bebida) do |form| %>
  <% if bebida.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bebida.errors.count, "error") %> prohibited this bebida from being saved:</h2>

      <ul>
        <% bebida.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
