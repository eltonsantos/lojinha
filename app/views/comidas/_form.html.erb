<%= form_with(model: comida) do |form| %>
  <% if comida.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comida.errors.count, "error") %> prohibited this comida from being saved:</h2>

      <ul>
        <% comida.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
